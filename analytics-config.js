/**
 * Analytics configuration.
 *
 * Fill in the values below once you create each account — steps in chat.
 * Leave a value as an empty string to skip that provider; analytics.js
 * checks for this and does nothing, so the site works fine either way.
 *
 * Commit this file normally — GitHub Pages serves the repo as-is, with
 * no build step, so anything gitignored here simply won't exist on the
 * live site. That's fine: neither ID below is a secret. They don't grant
 * access to anything and are visible in any browser's network tab the
 * moment the site loads, regardless of where they live in the repo.
 */
window.GA_MEASUREMENT_ID = '';       // e.g. 'G-XXXXXXXXXX' — GA4 → Admin → Data Streams → Web
window.CLOUDFLARE_BEACON_TOKEN = ''; // Cloudflare dashboard → Analytics & Logs → Web Analytics → Add site
